# Core Data Science & Analysis
pandas
numpy
scikit-learn
jupyter

# Deep Learning & GNN (Cloud Side)
torch
torch-geometric
# Jupyter kernel support for the project virtualenv
ipykernel
# PyG sampling backends used by LinkNeighborLoader / NeighborSampler are NOT pinned here,
# because they depend on the exact Python version, torch version, and CUDA/CPU build on each machine.
# Install wheels that EXACTLY match `python -c "import torch; print(torch.__version__)"`.
# Example for this repo's current environment:
#   pip install pyg-lib torch-sparse torch-scatter -f https://data.pyg.org/whl/torch-2.6.0+cu124.html
# CPU example:
#   pip install pyg-lib torch-sparse torch-scatter -f https://data.pyg.org/whl/torch-2.6.0+cpu.html
# Index of all builds: https://data.pyg.org/whl/
# If you see OSError / undefined symbol, uninstall and reinstall all three from the same -f URL as torch.
# These are required for notebooks 04 / 06 / 07 when using LinkNeighborLoader:
#   pyg-lib
#   torch-sparse
#   torch-scatter
sentence-transformers

# On-Device ML (Device Side)
MNN
tensorflow

# Data processing formats (based on existing clean_meta.parquet)
pyarrow
fastparquet
