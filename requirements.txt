# Core Data Science & Analysis
pandas
numpy
scikit-learn
jupyter

# Deep Learning & GNN (Cloud Side)
torch
torch-geometric
# LinkNeighborLoader / NeighborSampler needs pyg-lib AND a matching torch-sparse (or pyg-lib alone if your PyG build allows).
# Install wheels that EXACTLY match `python -c "import torch; print(torch.__version__)"` (e.g. 2.11.0+cu130):
#   pip install pyg-lib torch-sparse -f https://data.pyg.org/whl/torch-2.11.0+cu130.html
# CPU example:
#   pip install pyg-lib torch-sparse -f https://data.pyg.org/whl/torch-2.11.0+cpu.html
# Index of all builds: https://data.pyg.org/whl/
# If you see OSError/undefined symbol when importing pyg_lib, uninstall and reinstall both from the same -f URL as torch.
sentence-transformers

# On-Device ML (Device Side)
MNN
tensorflow

# Data processing formats (based on existing clean_meta.parquet)
pyarrow
fastparquet
networkx
matplotlib